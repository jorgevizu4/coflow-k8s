apiVersion: v1
kind: Service
metadata:
  name: coflow-backend
  labels:
    app: coflow-backend
spec:
  selector:
    app: coflow
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP