apiVersion: v1
kind: Service
metadata:
  name: coflow-backend
  labels:
    app: coflow-backend
spec:
  selector:
    app: coflow-backend
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP