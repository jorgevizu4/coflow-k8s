apiVersion: v1
kind: Service
metadata:
  name: coflow-frontend
  labels:
    app: coflow-frontend
spec:
  selector:
    app: coflow-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP